[project]
name = "agentfamily"
version = "0.1.0"
description = "A practice project for building a small handoff-driven AI family with the OpenAI Agents SDK"
requires-python = ">=3.12"
dependencies = [
    "openai-agents[litellm]",
    "openai",
    "python-dotenv"
]
